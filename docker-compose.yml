services:
  claude-agent:
    build:
      context: .
      dockerfile: Dockerfile.claude-agent
    container_name: claude-agent
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      # Project source
      - .:/workspace
      # Preserve node_modules inside container
      - node_modules:/workspace/node_modules
      # Claude config directory (plugins, settings, memory, skills — NOT auth tokens)
      # Do NOT mount .claude.json — it's a live file, host writes cause invalid JSON in container
      - ${HOME}/.claude:/home/claude/.claude
      # Resolve symlinks: ~/.claude/* → /Users/ericcai/dotfiles/claude/*
      # ~/.claude/ 內有 18 個 symlink 指向此絕對路徑，需原路掛載才能解析
      - ${HOME}/dotfiles/claude:/Users/ericcai/dotfiles/claude:ro
      # ccusage-tracker (SessionEnd hook 使用絕對路徑引用)
      - ${HOME}/.config/ccusage-tracker:/Users/ericcai/.config/ccusage-tracker
    environment:
      - DATABASE_URL=postgresql://maid:maid_dev_pw@postgres:5432/maid_development
      - JWT_SECRET=docker-dev-jwt-secret
      # Skip Prisma postinstall in container (run manually via entrypoint)
      - PRISMA_SKIP_POSTINSTALL_GENERATE=true
      # Playwright browsers pre-installed in base image
      - PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
    stdin_open: true
    tty: true
    deploy:
      resources:
        limits:
          memory: 4g

  postgres:
    image: postgres:17-alpine
    container_name: claude-agent-db
    environment:
      - POSTGRES_USER=maid
      - POSTGRES_PASSWORD=maid_dev_pw
      - POSTGRES_DB=maid_development
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U maid -d maid_development"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  node_modules:
  pgdata:
